File -2  created
git checkout -b feature
git add .
git commit -m "change done"
# Switch back to the main branch
git checkout main
git merge feature
git status
git add filename
git add.
git push origin feature
